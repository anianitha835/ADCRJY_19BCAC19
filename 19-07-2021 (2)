class Solution{
    static int map(int n, String keys[], int arr[], String s)
    {
        Map<String,Integer> map = new HashMap<>();
        
        for(int i =0;i<n;i++) {
            map.put(keys[i],arr[i]);
        }
        
        return map.get(s)==null?-1:map.get(s);
    }
}
